
# Local runtime config
config.yaml

# Local-only artifacts. local/value_smoke/ holds E2 comparison transcripts
# with conversation bodies and must never be committed.
local/

# Twin Extraction offline tooling input/output. Holds raw archive material,
# batch files, and extraction results and must never be committed.
runtime/twin_extraction/

# Windows / WSL metadata
*:Zone.Identifier
*.Zone.Identifier

# Python cache
__pycache__/
*.py[cod]

# Python packaging / tooling
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / frontend tooling
node_modules/
.vite/
*.tsbuildinfo
