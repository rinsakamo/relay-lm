# Renamed: RelayEMO Return-side Expression Design

The current design document has moved to [RelayEMO Return-side Expression Design](relayemo_return_side_expression_design.md).

The old `style_adapter` filename remains only as a compatibility redirect. RelayEMO emits bounded expression hints; it does not own the TTS or avatar adapters.
