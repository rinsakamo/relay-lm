# Moved: Runtime Compile Gate Design

The current design document has moved to [Runtime Compile Gate Design](architecture/runtime_compile_gate_design.md).

This path remains only as a compatibility redirect for historical links. New and current documentation must link directly to `docs/architecture/runtime_compile_gate_design.md`.
