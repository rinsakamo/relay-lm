# RelayLM 2.0 Cognitive IR S3-R4 frozen campaign

Owner: #2580  
Scientific owner: #2211  
Physical procedure authority: #2363

S3-R4 is the fresh citable-candidate campaign permitted only after the non-citable
P2 boundedness qualification in #2577. This repository binding does not itself
authorize a provider, model, GPU, llama.cpp server, or local artifact execution.

## Frozen identity

```text
schema = relaylm2-cognitive-ir-s3-prereg-v4
label  = relaylm2-cognitive-ir-s3-semantic-invariance-v4

shared    1813949056 599436490  759541357
null      1844644632 1939834448 1783561206
mismatch  1470423814 1347260951 1196431812
shift     1233805618 2217769    179280278

preregistration_sha256 =
deb10b356f1cfe4fd18275b790f1a5bcb5972b1b841e874c26a8827ffd20dae4
```

These are the already-precommitted #2530 seeds. They are not regenerated from
qualification or historical S3 outcomes.

## Scientific shape

R4 preserves S3-R3 except for the fresh R4 identity/seeds and the already-qualified
R4-scoped P2 prompt repair.

```text
regime order                    shared -> null -> mismatch -> shift
families                        3 per regime
semantic calls by shard         123 / 123 / 123 / 129
semantic calls total            498
exact /input_tokens requests    996
surface effect maximum          0.15
semantic effect minimum         0.20
semantic-surface gap minimum    0.15
```

P0 through P6 retain their frozen meanings. P4, P5, and P6 share one learned
formation completion, and P4/P6 canonical semantic equality remains a required
causal control. The surface perturbation, semantic intervention, and
option-value/query-family-shift panels remain inherited from S3-R3 unchanged.

## R4-scoped P2 repair

Only `P2_ORDINARY_SUMMARY` formation is substituted. R4 uses exactly:

```text
relaylm.v2_cognitive_ir_p2_boundedness_qual_v2.build_margin_p2_formation_messages
```

The builder preserves the legacy source packet and ordinary-summary semantics and
adds the predeclared compliance target:

```text
aim 60-80 whitespace-delimited words
aim <= 550 Unicode characters
```

Those values are prompt guidance, not an admission or efficacy threshold. The
unchanged hard mechanical admission is:

```text
finish_reason == stop
visible content nonempty
whitespace-delimited words <= 120
Unicode characters <= 800
reasoning absent/empty where exposed
```

A violation is terminal. There is no truncation, repair, retry, replay, reseed,
fallback, grammar/schema conversion, or arm-specific rescue. P3/P4 formation
messages and all later probes are inherited unchanged.

## Runtime contract

The physical owner must freshly attest the runtime/material binding before
execution. The repository-frozen request controls remain:

```text
context                  8192
parallel slots           1
context shift            disabled
reasoning_effort         none
temperature              0
request seed             null
max_output_tokens        1024
retry/replay/reseed       0
fallback                 0
```

The explicit WSL route is:

```text
python3 -m tools.v2_cognitive_ir_s3_r4_llama_cpp_wsl
```

It is a physical route and must not be invoked by CI or by #2580.

## Citable completion

The campaign begins incomplete and non-citable. It becomes a citable candidate
only if all four ordered shards complete under one exact repository/runtime
identity with the frozen 498/996 accounting and all material/lifecycle checks.
Any partial shard or fail-closed condition terminates the campaign as incomplete;
there is no continuation or skip-forward.

A completed R4 result remains evidence for #2211 only. `architecture_consequence`
is `NONE` at this boundary: a positive P4 result does not directly authorize a
Memory store, Structure store, ontology change, #2188 merge, or #2132 mutation.
