# RelayLM 2.0 semantic reconstruction physical adapter

Owner: #2723. Scientific preregistration: #2709. Repository science binding: #2722.

## Purpose

This surface makes the frozen semantic-reconstruction discriminator executable through the common #2660 llama.cpp physical infrastructure without authorizing a scientific run.

The scientific question remains exactly A vs B:

```text
A = canonical semantic sufficiency
B = bounded consumer accessibility
```

It does not test realized use (C) or operational efficacy (D), and no verdict changes RelayLM architecture.

## Ownership boundary

```text
#2660
  common physical HOW
  queue / clean checkout / persistent Python / GPU-port-process lifecycle /
  fresh protected-ref validation / target dispatch / generic receipts

#2211 + #2709
  scientific WHAT / preregistration

#2722
  zero-GPU scientific repository binding

#2723
  zero-GPU executable physical adapter

future separate exactly-once physical owner
  THIS RUN / exact scientific spend

#2668
  completed empirical result -> scoped Level-D reverse import only
```

#2723 itself authorizes no GPU, model, or semantic execution.

## Material-generation boundary

The exact 16 preregistered seed identities are repository-visible, but the corresponding scientific family objects must not exist during import, target discovery, CI, help, manifest construction, or mechanical preflight.

The official execution order is:

```text
fresh exact protected-v2 checkout
  -> common #2660 target dispatch
  -> owned llama.cpp lifecycle preflight
  -> /health + /v1/models + /props + /slots runtime attestation
  -> exact binary / GGUF / chat-template / context / slot identity
  -> synthetic exact input-token-counter preflight
  -> immutable FrozenConsumerIdentity
  -> ONLY THEN instantiate the 16 scientific K3 payloads
  -> derive P4/P6 mechanism-control serializations
  -> spend exactly 32 sequential semantic calls
  -> deterministic strict scoring + paired exact inference
  -> final live material/runtime binding check
  -> persist result + receipt
```

A runtime that cannot prove the frozen envelope stops before the scientific family generator is entered.

## Deterministic K3 material

The runtime-only scientific generator accepts only the exact `(family_index, preregistered_seed)` relation from #2709.

Each canonical payload has:

```text
operation = affine_permutation
permutation = [0,1,2,3]
width = 4
modulus = 10
exactly 3 nonzero offsets
active offset values in 1..3
opaque deterministic provenance handle(s)
```

The provenance handle contains no arm or answer hint. A separate synthetic helper rejects every preregistered scientific seed and is the only family-material path used by repository tests.

## Model-facing transport

Both arms use the same system prompt frozen by #2722. The only arm-dependent bytes are the ordinary serialized P4/P6 representation supplied as the user message.

The direct llama.cpp request contract is:

```text
endpoint = http://127.0.0.1:1234/v1
context = 8192
parallel semantic slots = 1
max_tokens = 256
temperature = 0
reasoning_effort = none
request seed = omitted / null
stream = false
response_format = absent
```

The primary discriminator deliberately uses ordinary text completion followed by the strict #2722 parser. Native JSON schema, representation-specific repair, hidden decoder help, fallback, retry, replay, reseed, model judge, and hidden repair are forbidden.

## Accounting

Scientific budget:

```text
families = 16
arms per family = 2
semantic calls = 32
exact input-token counter requests = 64
semantic retry/replay/reseed/fallback/judge/repair = 0
```

The llama.cpp exact input counter uses two non-generative requests per semantic call: one full request and one empty-content framing request.

Before the scientific material boundary, the transaction performs one synthetic counter preflight, costing exactly two additional non-generative input-token requests. Those two requests are mechanical preflight and are not part of the 64-request scientific accounting ledger.

Per scientific cell the result records raw completion, deterministic score diagnostics, semantic digest, representation bytes, exact input/output token counts when supplied by the runtime, one model-call unit, and wall-clock duration. Representation token count remains `null` unless an arm-symmetric exact measurement is available.

## Runtime identity

`FrozenConsumerIdentity` binds at least:

- clean RelayLM commit + tree;
- request model alias;
- exact llama.cpp upstream revision and build identity;
- exact GGUF path, SHA256, and ftype;
- chat-template SHA256 from the live runtime;
- context exactly 8192;
- exactly one slot;
- context shift disabled;
- max output 256;
- temperature 0;
- reasoning off;
- request seed null;
- semantic parallelism 1.

The common lifecycle also retains GPU/controller evidence, executable hash, launch command, log identity, and start/end live binding observations.

## Failure semantics

Before semantic spend, incompatible runtime or repository identity fails closed as a mechanical block. It is not evidence for or against accessibility.

Once a semantic provider attempt has occurred, a provider/protocol failure preserves all completed cell evidence and yields:

```text
PROTOCOL_RUNTIME_FAILURE_AFTER_SCIENTIFIC_SPEND
statistical_status = UNDERDETERMINED
architecture_consequence = NONE
```

A final runtime/material binding failure after spend likewise makes the authoritative semantic-reconstruction result V3/UNDERDETERMINED. No rescue run is authorized.

The generic outer transaction summary may still describe its mechanical disposition; after scientific spend the dedicated `semantic-reconstruction-result.json` is the authoritative scientific classification.

## Common runner target

The common physical registry exposes:

```text
v2:semantic-reconstruction
```

It routes to the WSL adapter, then the dedicated transaction, and only then the scientific runner. The target never points directly at a function that could instantiate scientific material before runtime identity is frozen.

## CI boundary

Repository CI is zero-GPU and zero-model-call. Tests use synthetic non-preregistered material only and mechanically prove:

- binding validation does not instantiate scientific family objects;
- invalid scientific `(index, seed)` admission fails before material generation;
- synthetic material has the frozen K3 geometry;
- runtime/counter identity is frozen without material generation;
- synthetic 16-family execution has exactly 32 sequential semantic calls and 64 counter requests;
- P4/P6 prompts are arm-symmetric except representation bytes;
- llama.cpp generation contains no request seed and no `response_format`;
- failure after a scientific provider attempt produces V3 and preserves partial cells;
- the common target routes through WSL/transaction #2660 HOW.

No exact preregistered scientific family is generated by these tests.

## Downstream

After #2723 is merged and post-merge CI is GREEN, a distinct exactly-once physical owner may freeze one fresh protected-v2 identity, exact runtime/model/GPU/artifact identities, empty destination, and spend state before handing the transaction to Local Codex.

A clean completed result may be reverse-imported to #2668 only at scoped Level-D with its source, treatment, held-fixed coordinates, raw paired result/statistics, Grand Null, forbidden inference, and architecture consequence NONE.

No direct Theory of Time or Cosmology export is authorized.

Refs #2211 #2660 #2668 #2709 #2722 #2723.
