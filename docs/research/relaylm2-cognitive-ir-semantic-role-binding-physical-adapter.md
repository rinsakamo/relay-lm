# E4-RB1 semantic-role binding physical adapter

Owner: #2834. Scientific parent: #2211. Preregistration: #2830. Repository binding: #2831.

## Status

This surface binds the already-frozen E4-RB1 scientific contract to the common protected-v2 llama.cpp physical runtime. It is a **zero-GPU repository adapter**. It does not authorize THIS RUN and does not contain an empirical E4-RB1 result.

Architecture consequence: **NONE**.

## Frozen experiment

Each of 24 fresh families has one canonical K3 semantic payload and three deterministic literal surfaces:

- `TYPED_MEMORY_STRUCTURE` (T)
- `NEUTRAL_ROLE_EXPLICIT` (R)
- `NEUTRAL_OPAQUE_ROLE_ALIASES` (O)

All three must decode to the same canonical payload and semantic digest before that family's semantic calls are admitted.

A completed panel contains exactly 72 sequential semantic completions and 144 scientific `/input_tokens` requests. The runtime may perform exactly two additional non-scientific `/input_tokens` requests as the pre-material structured-counter capability preflight.

The decoding envelope remains context 8192, max output 256, temperature 0, reasoning none/off, request seed omitted, stream false, and one semantic slot.

## Material boundary

Only seed identities exist before a live consumer is frozen. The official family generator requires a validated immutable `FrozenConsumerIdentity` and the exact preregistered `(index, seed)` relation. Family material is generated only after:

1. repository/common-generation/target/environment gates,
2. queue and external-runtime quiescence,
3. owned llama.cpp launch and live runtime binding,
4. exactly 2/2 non-scientific structured-counter preflight requests,
5. immutable consumer freeze.

Import, CI, target discovery, `--help`, manifest preparation, and ordinary preflight cannot generate an official family. Synthetic helpers reject every E4-RB1 preregistered seed.

## Measurement split

The physical adapter preserves #2831's prospective distinction:

- `wire_shape_valid` is an admission property;
- `semantic_domain_valid` is a scientific outcome.

A width-four integer `permutation` such as `[0,0,0,0]` is therefore wire-valid and domain-invalid. It counts as a wrong reconstruction rather than a measurement-floor failure. By contrast, malformed JSON, duplicate fields, wrong root/keys/types/width, prose/fences, or other wire failures invalidate completed-panel inference.

The native `response_format=json_schema`, `strict=true` schema is mechanical-only. It does not encode evaluator answer values, permutation bijection, semantic bounds, treatment identity, or an O-specific decoder.

## Inference

Only two confirmatory contrasts exist:

- H1: R versus O (semantic-role binding cost)
- H2: T versus R (typed-wrapper residual)

Both use within-family two-sided exact McNemar/sign tests. Holm-Bonferroni controls familywise alpha 0.05 across exactly H1 and H2. T versus O is descriptive only. There is no equivalence margin; non-rejection is not equivalence.

## Runtime path

The registered target is:

```text
v2:semantic-role-binding
```

It resolves through the protected common runner and resource lease `llama-cpp:local-gpu` to:

```text
tools.v2_cognitive_ir_semantic_role_binding_llama_cpp_wsl
 -> tools.v2_cognitive_ir_semantic_role_binding_llama_cpp_transaction
 -> tools.v2_cognitive_ir_semantic_role_binding_llama_cpp
 -> relaylm.v2_cognitive_ir_semantic_role_binding_physical
```

The adapter pins and verifies `relay-common-physical-g2` with aggregate:

```text
sha256:bb983011905bdd8b5393c2c3459b691289f5ced9a41561bb8dc7f642fa330b87
```

The target registry is outside that certified 13-path aggregate; adding this target does not mutate common g2.

## Failure discipline

Before scientific materialization, any unprovable repository, Python, common generation, target, llama.cpp, model/GGUF, context, slot, context-shift, reasoning, schema, `/input_tokens`, or artifact-destination identity fails closed as `INVALID_BEFORE_PHYSICAL_EXECUTION`.

After scientific spend begins, all completed/partial cells are preserved. Retry, replay, reseed, fallback, hidden repair, and model judge remain zero. An incomplete or wire-invalid panel is `MEASUREMENT_OR_RUNTIME_FAILURE_AFTER_SCIENTIFIC_SPEND` and no rescue significance is computed.

A later exactly-once physical owner must freshly bind protected v2, common generation, target, persistent Python, executable/model/template/tokenizer/hardware identities and a fresh empty artifact destination before invoking the common runner. #2834 itself performs no physical execution.
