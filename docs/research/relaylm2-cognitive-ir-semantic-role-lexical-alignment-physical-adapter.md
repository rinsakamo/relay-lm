# E4-LX1 lexical-role decomposition physical adapter

Owner: #2857. Scientific parent: #2211. Preregistration: #2847. Repository binding: #2848.

## Status

This surface binds the already-frozen E4-LX1 scientific contract to the common protected-v2 llama.cpp physical runtime. It is a **zero-GPU repository adapter**. It does not authorize THIS RUN and contains no empirical E4-LX1 result.

Architecture consequence: **NONE**.

## Frozen experiment

Each of 24 fresh families has one canonical K3 semantic payload and three deterministic neutral surfaces:

- `NEUTRAL_ROLE_EXACT_LEXEMES` (E)
- `NEUTRAL_ROLE_DESCRIPTIVE_NONLEXICAL` (D)
- `NEUTRAL_OPAQUE_ROLE_ALIASES` (O)

All three must decode to the same canonical payload and semantic digest before that family's semantic calls are admitted. D uses only `source_handles`, `transform_kind`, `index_reordering`, `additive_shifts`, and `modular_divisor`; no model-visible D/O decoder legend exists.

A completed panel contains exactly 72 sequential semantic completions and 144 scientific `/input_tokens` requests. The runtime may perform exactly two additional non-scientific `/input_tokens` requests as the pre-material structured-counter capability preflight.

The decoding envelope remains context 8192, max output 256, temperature 0, reasoning none/off, request seed omitted, stream false, and one semantic slot.

## Material boundary

Only seed identities exist before a live consumer is frozen. The official family generator requires a validated immutable `FrozenConsumerIdentity` and the exact preregistered `(index, seed)` relation. Family material is generated only after repository/common-generation/target/environment gates, queue and runtime quiescence, owned llama.cpp launch and live binding, exactly 2/2 non-scientific structured-counter requests, and immutable consumer freeze.

Import, CI, target discovery, `--help`, manifest preparation, and ordinary preflight cannot generate an official family. Synthetic helpers reject every E4-LX1 preregistered seed.

## Measurement split and inference

`wire_shape_valid` is an admission property; `semantic_domain_valid` is a scientific outcome. A shape-valid non-bijective permutation is therefore a wrong reconstruction, not a wire failure. The native strict JSON schema remains mechanical-only.

Only two confirmatory contrasts exist:

- H1: E versus D — literal output-key lexical-alignment effect
- H2: D versus O — semantic-role descriptiveness without exact key overlap

Both use within-family two-sided exact McNemar/sign tests, with Holm-Bonferroni across exactly H1 and H2 at familywise alpha 0.05. E versus O is descriptive only. There is no equivalence margin; non-rejection is not equivalence.

## Runtime path

The registered target is `v2:semantic-role-lexical-alignment`. It resolves through the protected common runner and resource lease `llama-cpp:local-gpu` to the dedicated E4-LX1 WSL wrapper, transaction, direct llama.cpp transport, and physical campaign module.

The adapter pins `relay-common-physical-g2` at aggregate `sha256:bb983011905bdd8b5393c2c3459b691289f5ced9a41561bb8dc7f642fa330b87`. The target registry is outside the certified common-generation surface; this adapter does not mutate common g2.

## Failure discipline

Before scientific materialization, any unprovable repository, Python, common generation, target, llama.cpp, model/GGUF, context, slot, context-shift, reasoning, schema, `/input_tokens`, or artifact-destination identity fails closed as `INVALID_BEFORE_PHYSICAL_EXECUTION`.

After scientific spend begins, all completed/partial cells are preserved. Retry, replay, reseed, fallback, hidden repair, and model judge remain zero. An incomplete or wire-invalid panel is `MEASUREMENT_OR_RUNTIME_FAILURE_AFTER_SCIENTIFIC_SPEND` and no rescue significance is computed.

A later exactly-once physical owner must freshly bind protected v2, common generation, target, persistent Python, executable/model/template/tokenizer/hardware identities and a fresh empty artifact destination before invoking the common runner. #2857 itself performs no physical execution.
