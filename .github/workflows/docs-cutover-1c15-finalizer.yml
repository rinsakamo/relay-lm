name: Docs cutover 1C-15 finalizer

on:
  pull_request:
    paths:
      - ".github/workflows/docs-cutover-1c15-finalizer.yml"

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package
        run: python -m pip install -e .
      - name: Finalize receipt
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('docs/evidence/migrations/documentation-hard-cutover-receipt.md')
          body = path.read_text(encoding='utf-8')
          marker = '### C1C15-001 — LAT-1 Latency Measurement completion report'
          start = body.index(marker)
          old = '  all_github_actions: pending'
          position = body.index(old, start)
          assert position < body.index('## Pending batches', start)
          body = body[:position] + '  all_github_actions: passed' + body[position + len(old):]
          path.write_text(body, encoding='utf-8')
          PY
      - name: Validate final state
        run: |
          python -m compileall -q relaylm scripts
          PYTHONPATH=.:scripts python scripts/relaylm_lat1_timing_smoke.py
          PYTHONPATH=.:scripts python scripts/relaylm_lat1_timing_security_smoke.py
          PYTHONPATH=.:scripts python scripts/relaylm_lat1_bench_smoke.py
          PYTHONPATH=.:scripts python scripts/relaylm_mvp_completion_report_smoke.py --check-model docs/evidence/implementation/lat1_latency_measurement_completion_report.md
          PYTHONPATH=.:scripts python scripts/relaylm_mvp_completion_report_pr_link_smoke.py
          PYTHONPATH=.:scripts python scripts/relaylm_docs_link_check.py
          PYTHONPATH=.:scripts python scripts/relaylm_docs_semantic_audit.py
          PYTHONPATH=.:scripts python scripts/relaylm_documentation_current_boundary_smoke.py
          git diff --check
      - name: Commit receipt finalization
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/evidence/migrations/documentation-hard-cutover-receipt.md
          git commit -m "docs: finalize Cutover 1C-15 validation state"
          git push origin HEAD:${{ github.head_ref }}
