name: Documentation completion report files

on: pull_request

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/relaylm_mvp_completion_report_smoke.py --check-all
