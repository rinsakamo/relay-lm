name: Documentation completion report model

on:
  workflow_dispatch:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/relaylm_mvp_completion_report_smoke.py --check-model
